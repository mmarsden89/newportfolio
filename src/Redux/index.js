import { setColorBool } from "./colorBoolSlice";
import { setColorRangeBool } from "./colorRangeBoolSlice";
import { replaceColorValue } from "./colorValueSlice";
import { setDay } from "./daySlice";
import { setDisplayMessage } from "./displayMessageSlice";
import { setFontsBool } from "./fontsBoolSlice";
import { setMsgBool } from "./msgBoolSlice";
import { setPrimaryFont } from "./primaryFontSlice";
import { setSecondaryFont } from "./secondaryFontSlice";
import { setShowMessage } from "./showMessageSlice";
import { replace } from "./slideArraySlice";
import { setToolbar } from "./toolbarSlice";
import { setZipcode } from "./zipcodeSlice";
import {
    increment,
    decrement,
    incrementByAmount,
    makeZero,
} from "./currentSlideSlice";

export {
    setColorBool,
    setColorRangeBool,
    replaceColorValue,
    setDay,
    setDisplayMessage,
    setFontsBool,
    setMsgBool,
    setPrimaryFont,
    setSecondaryFont,
    setShowMessage,
    replace,
    setToolbar,
    setZipcode,
    increment,
    decrement,
    incrementByAmount,
    makeZero,
};
